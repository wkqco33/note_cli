# 보안 정책

`note_cli`는 인증 토큰과 비밀번호를 로컬에서 암호화해 보관하며, 원격 API와 통신하는 CLI 도구입니다. 이 프로젝트의 보안 취약점을 발견하셨다면 아래 절차에 따라 알려주시기 바랍니다.

## 신고 절차

**중요:** 취약점을 공개 이슈(issue)나 PR에 작성하지 마세요. 다른 사용자에게도 피해가 갈 수 있습니다.

1. GitHub의 [Private Vulnerability Reporting](https://github.com/wkqco33/note_cli/security/advisories/new) 기능을 사용해 보고해주세요. (권장)
2. 만약 GitHub 리포지토리에서 보고가 불가능한 경우, 리포지토리 소유자에게 이메일로 알려주세요. 신고 시 다음 정보를 포함해 주세요:
   - 취약점 종류와 발생 위치(파일/함수)가 있으면
   - 재현 단계
   - 영향 범위와 심각도 추정
   - 가능하면 PoC(Proof of Concept)

### 보고 시 포함하면 좋은 정보

- 실행 환경: OS, Go 버전, 아키텍처
- 사용 중인 `note_cli` 버전(또는 커밋 해시)
- 관련 설정(비밀값 제외)

## 응답 시간

- **초기 응답**: 보고 후 영업일 기준 **5일 이내**에 접수 확인과 초기 의견을 드립니다.
- **진행 공유**: 확인 결과와 조치 일정을 정기적으로 공유합니다.
- **공개**: 취약점이 확인되면 수정 릴리스가 준비된 후, 신고자와 협의하여 공개합니다. 조치 전까지는 신고 내용을 비공개로 유지합니다.

## 보안 관련 설계 요약

`note_cli`의 인증/비밀값 처리 방식을 아래에 요약합니다. 신고 전 참고하시면 도움이 됩니다.

- **토큰 저장**: 로그인 후 `~/.config/note_cli/config.yaml`에 액세스/리프레시 토큰과 자동 로그인 비밀번호를 저장합니다. 값은 평문이 아닌 **`enc:` 접두사와 함께 플랫폼별 암호화**하여 기록합니다 (`config/secure.go`, `config/config.go`).
  - Windows: DPAPI (`config/secure_windows.go`)
  - 그 외: AES-256-GCM + 시스템 파생 키 (`config/secure_fallback.go`)
  - `config.yaml` 파일 권한은 `0600`으로 생성됩니다.
- **네트워크**: 원격 API 서버와 통신하며 JWT 기반 인증을 사용합니다.

## 지원 버전

현재 보안 패치는 최신 릴리스에 적용됩니다. 패치가 적용되는 구간에 대해서는 아래 표를 참고하세요.

| 버전        | 지원 상태   |
| ----------- | ----------- |
| 최신 릴리스 | 지원        |
| 이전 릴리스 | 최선의 노력 |

## 기타

- 이 저장소의 보안 설정(`.gitignore`, CI의 gitleaks 스캔 등)에 대한 개선 제안도 환영합니다.
- 감사 및 보안 관련 문의는 위 신고 채널을 이용해 주세요.
